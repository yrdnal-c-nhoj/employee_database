name: Wake Up Render
on:
  schedule:
    - cron: '*/10 * * * *' # Runs every 10 minutes
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Render URL
        run: curl -s https://emp-data-wserv.onrender.com > /dev/null