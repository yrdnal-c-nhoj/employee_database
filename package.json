{
  "name": "employee-database",
  "version": "1.0.0",
  "description": "MERN Employee Database Application",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm install && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
